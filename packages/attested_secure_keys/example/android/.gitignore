# NOTE: the Gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) is
# intentionally committed — the Firebase Test Lab workflow (device-tests.yml)
# invokes ./gradlew directly on a fresh checkout, so the wrapper must be tracked.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
